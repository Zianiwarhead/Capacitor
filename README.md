# Capacitor

A project for managing clipboard operations.

## Setup
1. Install dependencies: `pip install -r requirements.txt`
2. Run the main script: `python src/clipboard/clipboard_manager.py`

## Tests
Tests are located in the `tests/` directory.